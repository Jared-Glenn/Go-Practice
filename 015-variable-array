package main
import "fmt"

func main() {
  var n int
  fmt.Scanln(&n)
  
  //your code goes here
  res := make([]int, 0)
  
  var j int
  
  for i:=0;i<n;i++ {
      fmt.Scanln(&j)
      res = append(res, j)
  }
  
  fmt.Println(res)
}
